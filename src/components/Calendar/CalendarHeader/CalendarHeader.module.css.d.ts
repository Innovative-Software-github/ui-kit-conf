declare const cls: {
  readonly container: string;
  readonly contolButtons: string;
  readonly monthButton: string;
  readonly yearButton: string;
  readonly prevButton: string;
  readonly nextButton: string;
};

export = cls;
